{
    "extends": "./tsconfig.common.json",
    "compilerOptions": {
        "outDir": "out",
        "rootDir": "src",
    },
    "exclude": [
        "node_modules",
        "modules",
        "out",
        "__tests__" // Test files can be run as `.ts` files with jest, no need to compile, can be ignored.
    ],
    "references": [
        {"path": "./modules/psl-parser"}
    ]
}
